// the stament  is a line of code for declare instructions and the blocks are a set of instructions inside the {}

/*printf("Hello world!");
costoPizzas = PIZZACOST * numberOfSlices;
printf("Total bill is: %c %f", NEWLINE, costoPizzas );


{
    printf("Hello world!");
    costoPizzas = PIZZACOST * numberOfSlices;
    printf("Total bill is: %c %f", NEWLINE, costoPizzas);

}
ls
*/
   